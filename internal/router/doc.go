// Package router implements the central control plane: the public
// OpenAI-compatible ingress, API-key authentication, request correlation
// and dispatch over the NNG mesh to enrolled worker nodes.
package router
